export const profileStatusSelector = (state) => state.profile.status;

export const profileUserSelector = (state) => state.profile.user;

export const profileFriendsSelector = (state) => state.profile.friends.friends;

export const profileFriendsStatusSelector = (state) => state.profile.friends.status;
